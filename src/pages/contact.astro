---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Gosvindraj | Contact" description="Get in touch with Gosvindraj. Open to collabs, questions, and interesting problems." hideFooter={true}>
  <section id="contact">

    <div class="contact-head">
      <div class="ch-top">
        <span class="m-label"><span class="lb">[</span> contact <span class="lb">/</span> open channel <span class="lb">]</span></span>
        <span class="m-label"><span class="dot-live" aria-hidden="true"></span> open to collabs</span>
      </div>
      <h1 class="ch-display display">
        <span class="line-mask"><span class="line">say <span class="serif acid">hi.</span></span></span>
      </h1>
    </div>

    <div class="contact-body">

      <div class="contact-form-col" id="contact-form-wrapper">
        <h2 class="sr-only" id="form-title">Send a message</h2>
        <form id="contact-form" autocomplete="off">
          <div class="field">
            <label for="name" class="field-label m-label">01 <span class="lb">/</span> name</label>
            <input type="text" id="name" name="name" placeholder="what do i call you?" required />
          </div>
          <div class="field">
            <label for="email" class="field-label m-label">02 <span class="lb">/</span> email</label>
            <input type="email" id="email" name="email" placeholder="where do i reply?" required />
          </div>
          <div class="field">
            <label for="message" class="field-label m-label">03 <span class="lb">/</span> message</label>
            <textarea id="message" name="message" rows="5" placeholder="what's on your mind?" required></textarea>
          </div>
          <button type="submit" class="btn-solid" data-magnetic>
            send it <span aria-hidden="true">&rarr;</span>
          </button>
        </form>
        <div id="response-message" role="status"></div>
      </div>

      <aside class="contact-aside">
        <p class="aside-note">
          collab idea, weird question, or just want to argue about
          whether AI agents need wallets? all valid reasons.
        </p>

        <div class="aside-block">
          <span class="m-label">direct</span>
          <a href="mailto:gosh@gosvindraj.com" class="aside-mail">gosh@gosvindraj.com</a>
        </div>

        <div class="aside-block">
          <span class="m-label">elsewhere</span>
          <ul class="aside-links">
            <li><a href="https://www.linkedin.com/in/gosvindraj-m-3306b9280/" target="_blank" rel="noopener">linkedin <span aria-hidden="true">&#8599;</span></a></li>
            <li><a href="https://www.instagram.com/gosv7ndraj/" target="_blank" rel="noopener">instagram <span aria-hidden="true">&#8599;</span></a></li>
            <li><a href="https://www.facebook.com/gosvindraj.fuzz/" target="_blank" rel="noopener">facebook <span aria-hidden="true">&#8599;</span></a></li>
            <li><a href="https://github.com/Gosvindraj" target="_blank" rel="noopener">github <span aria-hidden="true">&#8599;</span></a></li>
          </ul>
        </div>

        <div class="aside-block">
          <span class="m-label">location</span>
          <span class="aside-plain">penang, malaysia (gmt+8)</span>
        </div>
      </aside>

    </div>
  </section>
</BaseLayout>

<style>
  #contact {
    min-height: calc(100vh - var(--nav-height));
    padding: clamp(1.5rem, 4vh, 2.5rem) var(--gutter) clamp(1.25rem, 3vh, 2rem);
    display: flex;
    flex-direction: column;
    gap: clamp(1.25rem, 3.5vh, 2rem);
  }

  /* on desktop, cap the whole page to one viewport instead of
     letting it grow past it — mirrors the home page treatment */
  @media (min-width: 821px) {
    #contact {
      height: calc(100vh - var(--nav-height));
      overflow: hidden;
    }

    .contact-head { flex-shrink: 0; }

    .contact-body {
      flex: 1;
      min-height: 0;
    }
  }

  .contact-head {
    display: flex;
    flex-direction: column;
    gap: clamp(1rem, 3vh, 1.75rem);
  }

  .ch-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .ch-top .dot-live {
    display: inline-block;
    width: 6px;
    height: 6px;
    background: var(--acid);
    margin-right: 0.4rem;
    animation: pulse-acid 2.2s ease-in-out infinite;
  }

  .ch-display { font-size: clamp(2.6rem, min(9vw, 13vh), 7rem); }

  .line-mask {
    display: block;
    overflow: hidden;
    padding-bottom: 0.08em;
    margin-bottom: -0.08em;
  }

  .line { display: block; }

  .contact-body {
    display: grid;
    grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
    gap: clamp(2rem, 5vw, 4rem);
    align-items: start;
    border-top: 1px solid var(--line);
    padding-top: clamp(1.25rem, 3vh, 2rem);
  }

  /* ── form ─────────────────────────────────── */
  #contact-form {
    display: flex;
    flex-direction: column;
    gap: clamp(1.1rem, 3vh, 1.75rem);
  }

  .field {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    border-bottom: 1px solid var(--line);
    transition: border-color var(--transition);
    padding-bottom: 0.25rem;
  }

  .field:focus-within { border-color: var(--acid); }

  .field-label { color: var(--faint); }

  .field:focus-within .field-label { color: var(--acid); }

  .field input,
  .field textarea {
    width: 100%;
    background: transparent;
    border: none;
    outline: none;
    padding: 0.35rem 0 0.85rem;
    font-family: var(--font-sans);
    font-size: clamp(1.05rem, 2vw, 1.3rem);
    font-weight: 500;
    letter-spacing: -0.01em;
    color: var(--ink);
    resize: none;
    cursor: none;
  }

  .field input:-webkit-autofill,
  .field input:-webkit-autofill:hover,
  .field input:-webkit-autofill:focus {
    -webkit-box-shadow: 0 0 0 1000px var(--bg) inset;
    -webkit-text-fill-color: var(--ink);
    caret-color: var(--ink);
  }

  .field input::placeholder,
  .field textarea::placeholder {
    color: var(--faint);
    font-weight: 400;
  }

  #contact-form button[type="submit"] {
    align-self: flex-start;
    will-change: transform;
  }

  /* response */
  #response-message {
    display: none;
    margin-top: 1.5rem;
    padding: 1rem 1.25rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    letter-spacing: 0.04em;
    line-height: 1.6;
  }

  #response-message.response-success {
    display: block;
    border: 1px solid rgba(155, 109, 206, 0.4);
    background: var(--acid-dim);
    color: var(--acid);
  }

  #response-message.response-error {
    display: block;
    border: 1px solid rgba(235, 87, 87, 0.35);
    background: rgba(235, 87, 87, 0.08);
    color: #eb5757;
    animation: fadeOut 4s forwards;
  }

  .fade-out { opacity: 0; transition: opacity 0.4s ease; }
  .fade-in  { opacity: 0; display: block; animation: fadeIn 0.4s ease forwards; }

  @keyframes fadeIn  { to { opacity: 1; } }
  @keyframes fadeOut {
    0%, 75% { opacity: 1; }
    100%    { opacity: 0; }
  }

  /* ── aside ────────────────────────────────── */
  .contact-aside {
    display: flex;
    flex-direction: column;
    gap: clamp(1.1rem, 2.6vh, 1.75rem);
  }

  .aside-note {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: clamp(1rem, min(1.8vw, 2.4vh), 1.2rem);
    color: var(--dim);
    line-height: 1.55;
  }

  .aside-block {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .aside-mail {
    font-family: var(--font-sans);
    font-stretch: 112%;
    font-weight: 700;
    font-size: clamp(1.1rem, 2.4vw, 1.5rem);
    letter-spacing: -0.02em;
    color: var(--ink);
    transition: color var(--transition);
    word-break: break-all;
  }

  .aside-mail:hover { color: var(--acid); }

  .aside-links {
    list-style: none;
    display: flex;
    flex-direction: column;
  }

  .aside-links a {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    padding: 0.4rem 0;
    border-bottom: 1px solid var(--line);
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--dim);
    transition: color var(--transition), padding-left var(--transition);
  }

  .aside-links a:hover { color: var(--acid); padding-left: 0.5rem; }

  .aside-plain {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--dim);
  }

  @media (max-width: 780px) {
    .contact-body { grid-template-columns: 1fr; }
    #contact-form button[type="submit"] { align-self: stretch; justify-content: center; }
  }
</style>

<script>
  import gsap from "gsap";

  const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  function initContact() {
    const head  = document.querySelector<HTMLElement>(".contact-head");
    const body  = document.querySelector<HTMLElement>(".contact-body");
    if (!head) return;

    if (!reduced) {
      const lines = head.querySelectorAll<HTMLElement>(".line");
      const top   = head.querySelector<HTMLElement>(".ch-top");

      gsap.set(top, { opacity: 0 });
      gsap.set(body, { opacity: 0, y: 24 });
      gsap.set(lines, { yPercent: 110 });

      gsap.timeline({ delay: 0.15 })
        .to(lines, { yPercent: 0, duration: 0.85, stagger: 0.12, ease: "power4.out" })
        .to(body, { opacity: 1, y: 0, duration: 0.6, ease: "power3.out" }, "-=0.4")
        .to(top, { opacity: 1, duration: 0.5, ease: "power2.out" }, "-=0.3");
    }

    /* magnetic submit button */
    const submitBtn = document.querySelector<HTMLElement>("[data-magnetic]");
    if (submitBtn && !reduced) {
      submitBtn.addEventListener("mousemove", (e) => {
        const rect = submitBtn.getBoundingClientRect();
        gsap.to(submitBtn, {
          x: (e.clientX - (rect.left + rect.width / 2)) * 0.35,
          y: (e.clientY - (rect.top + rect.height / 2)) * 0.35,
          duration: 0.3,
          ease: "power2.out",
        });
      });
      submitBtn.addEventListener("mouseleave", () => {
        gsap.to(submitBtn, { x: 0, y: 0, duration: 0.5, ease: "elastic.out(1, 0.5)" });
      });
    }
  }

  /* ── form submit ─────────────────────────── */
  function initForm() {
    document.getElementById("contact-form")?.addEventListener("submit", async function (event) {
      event.preventDefault();

      const form    = document.getElementById("contact-form")         as HTMLFormElement;
      const msg     = document.getElementById("response-message")     as HTMLElement;

      const name    = (form.querySelector<HTMLInputElement>("#name"))?.value ?? "";
      const email   = (form.querySelector<HTMLInputElement>("#email"))?.value ?? "";
      const message = (form.querySelector<HTMLTextAreaElement>("#message"))?.value ?? "";

      try {
        const res = await fetch("/api/contact", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ name, email, message }),
        });

        if (res.ok) {
          form.classList.add("fade-out");
          setTimeout(() => {
            form.style.display = "none";
            msg.className = "response-success fade-in";
            msg.textContent = "message sent. i'll get back to you soon.";
          }, 400);
        } else {
          msg.className = "response-error";
          msg.textContent = "something went wrong. please try again.";
          setTimeout(() => { msg.className = ""; msg.textContent = ""; }, 4000);
        }
      } catch {
        msg.className = "response-error";
        msg.textContent = "something went wrong. please try again.";
        setTimeout(() => { msg.className = ""; msg.textContent = ""; }, 4000);
      }
    });
  }

  document.addEventListener("astro:page-load", initContact);
  document.addEventListener("astro:page-load", initForm);
</script>
